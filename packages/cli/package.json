{
  "name": "@august/monori-cli",
  "description": "👻⚙️ CLI scripts for Monori to run and boot faster",
  "version": "1.0.0-alpha.0",
  "license": "MIT",
  "author": "August <august@augu.dev>",
  "repository": "https://github.com/auguwu/i18n/tree/expiremental/postgres/packages/i18n-cli",
  "scripts": {
    "build:watch": "yarn lint && yarn clean && yarn tsc -w",
    "prepare": "rm -fr node_modules/@augu/logging/node_modules",
    "compile": "yarn tsc",
    "build": "yarn lint && yarn clean && yarn compile && cp -r build/ ../../cli",
    "clean": "rm -fr build",
    "lint": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "@augu/immutable": "0.2.6",
    "@terminalfreaks/clover": "1.1.0",
    "leeks.js": "0.0.8"
  },
  "devDependencies": {
    "@augu/eslint-config": "1.6.1",
    "@types/node": "14.0.19",
    "@typescript-eslint/eslint-plugin": "3.6.0",
    "@typescript-eslint/parser": "3.6.0",
    "eslint": "7.4.0",
    "typescript": "3.9.6"
  }
}